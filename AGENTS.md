# devflow

> 本项目用 devflow——纯文档编排 openspec + superpowers 的 6 阶段编程框架。

跑 `/devflow:brainstorm` 开始。完整 workflow + 全局规则 + 每阶段 checklist 已通过 `opencode.json` 的 `instructions` 从 `docs/devflow-rules.md` **自动加载**到 context(与 AGENTS.md 合并)。

> 本文件是**瘦桩**:`/init` 若"原地改进"只影响此桩;框架真内容在 `docs/devflow-rules.md`,`/init` 不碰。误改后 `git restore AGENTS.md`。

设计文档见 `docs/devflow-design.md`。
