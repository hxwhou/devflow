# devflow

> 纯文档编排 openspec + superpowers 的编程框架。入口唯一 `/devflow:brainstorm`(代理自判 fast-track / full,见 rules §规模判定)。

规则已通过 `opencode.json` 的 `instructions` 从 `devflow-rules.md` 自动加载到 context。

设计文档见 `docs/devflow-design.md`。
